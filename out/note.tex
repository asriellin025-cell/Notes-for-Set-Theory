\documentclass[12pt, a4paper, oneside]{article}
\usepackage{geometry}
\geometry{
    a4paper,
    top=2.5cm,
    bottom=2.5cm,
    left=2.5cm,
    right=2.5cm
}
\usepackage{amsmath, amssymb, amsfonts, amsthm, mathtools, bm, mathrsfs, tabularx}           

\title{\textbf{Notes on Elements of Set Theory}}
\author{Lin}
\date{\today}

\begin{document}
    \maketitle
    \newpage
    \theoremstyle{plain}
    \newtheorem*{remark}{Remark}
    \newtheorem{theorem}{Theorem}
    \theoremstyle{definition}
    \newtheorem{axiom}{Axiom}
    \newtheorem{definition}{Definition}
    
    \begin{theorem}
        $B\subseteq C \implies \mathscr{P}(B) \subseteq \mathscr{P}(C) $
    \end{theorem}
    
    \begin{proof}
        By def $\mathscr{P}(B) = \left\{ x \mid x \subseteq B \right\}$

        $\forall x, x \in \mathscr{P}(B)$, Then, $x \subseteq B \subseteq C$

        $\implies x \subseteq C$
    
        $\implies x \in \mathscr{P}(C)$ By def $\mathscr{P} (C) = \left\{ x \mid x \subseteq C \right\}$

        Thus, $\mathscr{P}(B) \subseteq \mathscr{P}(C)$
    \end{proof}

    \begin{theorem}
        A is a finite set,$|A|=n \implies |\mathscr{P}(A)| = 2^n$
    \end{theorem}
    
    \begin{proof}
        Consider taking $i$ elements from A to build a subset.

        Then $i \in \left\{ 0, \dots, n\right\}$, and the number of that kind of subsets is $\binom{n}{i}$.

        (if $i = 0$, the subset is $\emptyset$; if $i = n$, the subset is A itself)

        $\implies |\mathscr{P}(A)| = \sum_{i = 0}^{n}\binom{n}{i}$

        $\implies |\mathscr{P}(A)| = 2^n$, By Binomial Theorem, consider $(1+1)^n$
    \end{proof}

    \begin{theorem}
        $x, y \in B \implies \left\{ \left\{ x\right\},\left\{x, y\right\} \right\} 
        \in \mathscr{P}(\mathscr{P}(B))$
    \end{theorem}
    
    \begin{proof}
        $x, y \in B$

        $\implies \left\{x\right\}, \left\{ x, y\right\} \in \mathscr{P}(B)$

        $\implies \left\{\left\{x\right\}, \left\{x, y\right\}\right\} \subseteq \mathscr{P}(B)$

        Thus, by def $\left\{\left\{x\right\}, \left\{x, y\right\}\right\} \in \mathscr{P}(\mathscr{P}(B))$
    \end{proof}

    \begin{remark}
        A simple way to calculate the rank of set (which finite) is to count the deepest nested brackets until meet atom or $\emptyset$.
    \end{remark}

    \begin{theorem}
        $\forall \alpha \in \mathbb{N},\alpha \le 3, V_{\alpha + 1} = A \cup \mathscr{P}(V_{\alpha})$
    \end{theorem}
    
    \begin{proof}
        $V_0 = A$

        By def, $V_{n+1} = V_n \cup \mathscr{P}(V_n)$
        
        $V_1 = V_0 \cup \mathscr{P}(V_0) = A \cup \mathscr{P}(V_0)$

        $V_2 = V_1 \cup \mathscr{P}(V_1) = A \cup \mathscr{P}(V_0) \cup  \mathscr{P}(V_1) = A \cup \mathscr{P}(V_1)$
        
        $\mathscr{P}(V_0) \cup  \mathscr{P}(V_1) =  \mathscr{P}(V_1)$,
        By $\mathscr{P}(V_0) \subseteq \mathscr{P}(V_1)$ since $V_0 \subseteq V_1$, by Theorem 1.

        $V_3 = V_2 \cup \mathscr{P}(V_2) = A \cup \mathscr{P}(V_1) \cup  \mathscr{P}(V_2)$ Similarly,

        $V_3 = A \cup \mathscr{P}(V_2)$
    \end{proof}

    \begin{remark}
        We can use mathematical induction to make it to $\forall \alpha \in \mathbb{N}$
    \end{remark}

    \begin{remark}
        All sets are classes, 
        
        $\{x \mid \phi(x)\}$ is appropriately is class-builder, where $\phi(x)$ is open formula.

        Classes can not $\in$ themselves.
    \end{remark}

    \begin{remark}
        Definition is like an abbreviation of syntax,
        
        and it seems that it must be iff statements.
    \end{remark}

    \begin{remark}
        $\forall x (x\in B \iff \phi(x))  \iff B = \{x\mid \phi(x)\}$
    \end{remark}

    \begin{axiom}
        Extensionality Axiom
        $$\forall A \forall B ,(\forall x, x \in A \iff x \in B) \implies A = B$$
    \end{axiom}

    \begin{axiom}
        Empty Set Axiom
        $$\exists A \forall x, x \notin A$$
        \begin{remark}
            Does $\emptyset$ act like a unique generator in terms of abstract algebra?

             (e.g. similar as prime numbers can be used to generate whole $\mathbb{N}$ under multiplication)
        \end{remark}
    \end{axiom}

    \begin{axiom}
        Pairing Axiom
        $$\forall A \forall B \exists C \forall x(x \in C \iff x=A \vee x=B)$$ 
    \end{axiom}

    \begin{axiom}
        Union Axiom
        $$\forall A \exists B \forall x(x \in B \iff (\exists b \in A)x\in b)$$
        \begin{remark}
            Compare with $\forall A \forall B \exists C \forall x(x\in C \iff x\in A \vee x\in B)$,

            Which mentioned in previous page of the book (Page 18), named Preliminary Form. 
            Preliminary Form build union as a binary operator, similar to the Pairing Axiom.
            While the problem of Preliminary Form is can not tackle infinity sets 
            (you have to nest the union operation infinity many times to do that, which is illegal.
             A Well-Formed Formula in First-Order Classical Logic should have finite length)
        \end{remark}
    \end{axiom}

    \begin{axiom}
        Power Set Axiom
        $$\forall A \exists B \forall x(x\in B \iff x \subseteq A)$$
    \end{axiom}

    \begin{axiom}
        Subset Axiom
        $$\forall t_1 \dots \forall t_n \forall c \exists B \forall x(x\in B \iff x \in c \wedge \phi(x,t_1,\dots,t_n))$$
        \begin{remark}
            It is an axiom schema, where $\phi$ is a first-order formula;
            
            The number of subset axioms would be $\aleph_0$;

            $P(x)$ (predicate) includes $\phi(x)$ (open formula) and $\sigma$ (closed formula).
        \end{remark}
    \end{axiom}
      
    \begin{theorem}
        $x\in A \implies x \subseteq \bigcup A$
    \end{theorem}
    
    \begin{proof}
        $x \subseteq \bigcup A \iff \forall y( y \in x \implies y\in \bigcup A)$

        Statement equivalent to $x\in A \implies \forall y( y \in x \implies y\in \bigcup A)$

        By def $\bigcup A = \{y \mid (\exists b \in A) y\in b \}$

        Notice $\exists b, b = x$
    \end{proof}

    \begin{theorem}
        $A \subseteq B \implies \bigcup A \subseteq \bigcup B$
    \end{theorem}
    
    \begin{proof}
        This proof includes guidance on conceptual approaches from an LLM.

        $\forall x \in A, x\in B$

        Then $\forall y \in \bigcup A, \exists x \in A, y \in x$
        
        Then $y\in x \wedge x\in A$

        Since $A \subseteq B$, $x\in B$

        By Theorem 5, $x\in B \implies x \subseteq \bigcup B$

        Then $y \in \bigcup B$
    \end{proof}

    \begin{theorem}
        $\forall x \in A, x \subseteq B \implies \bigcup A \subseteq B$
    \end{theorem}
    
    \begin{proof}
        $\forall  y \in \bigcup A$, By def $\bigcup A = \{y\mid (\exists b \in A)y\in b\}$

        $\forall x\in A, x\subseteq B$
        
        Then $b\subseteq B$, $y\in B$
    \end{proof}

    \begin{theorem}
        $\forall A, \bigcup \mathscr{P}(A) = A$
    \end{theorem}
    
    \begin{proof}
        By def $\mathscr{P}(A) = \{x \mid x \subseteq A\}$

        $\bigcup \mathscr{P}(A) = \{y\mid (\exists b \in \mathscr{P}(A))y\in b\}$

        $(\subseteq)$ Notice $A \in \mathscr{P}(A)$

        Then $\bigcup \mathscr{P}(A) = \{y \mid y \in A\}$

        $A \subseteq \bigcup \mathscr{P}(A)$

        $(\supseteq)$ $\forall x \in \mathscr{P}(A), x \subseteq A$ By def

        Then $\bigcup \mathscr{P}(A) \subseteq A$ By Theorem 7

        Thus $\forall A, \bigcup \mathscr{P}(A) = A$
    \end{proof}

    \begin{theorem}
        $\forall A \forall B, \mathscr{P}(A) \cap \mathscr{P}(B) = \mathscr{P}(A \cap B)$
    \end{theorem}

    \begin{proof}
        By def $A \cap B = \{x\mid x\in A \wedge x \in B\}$

        $\mathscr{P}(A) = \{x \mid x \subseteq A\}$

        $\mathscr{P}(A) \cap \mathscr{P}(B) = \{x\mid x\in \mathscr{P}(A) \wedge x \in \mathscr{P}(B) \}$

        $\mathscr{P}(A \cap B) = \{x\mid x \subseteq A \cap B\}$

        $(\subseteq)$ $\forall x \in \mathscr{P}(A) \cap \mathscr{P}(B), x \subseteq A \wedge x \subseteq B$ 

        Then $x\subseteq A \cap B$

        Then $\mathscr{P}(A) \cap \mathscr{P}(B) \subseteq \mathscr{P}(A \cap B)$

        $(\supseteq)$ $\forall x \in \mathscr{P}(A \cap B), x \subseteq A \cap B$

        $x\subseteq A \wedge x \subseteq B$

        $x\in \mathscr{P}(A) \wedge x \in \mathscr{P}(B)$

        $x \in \mathscr{P}(A) \cap \mathscr{P}(B)$

        Then $\mathscr{P}(A) \cap \mathscr{P}(B) \supseteq \mathscr{P}(A \cap B)$

        Thus $\mathscr{P}(A) \cap \mathscr{P}(B) = \mathscr{P}(A \cap B)$
    \end{proof}

    \begin{theorem}
        $\mathscr{P}(A) \cup  \mathscr{P}(B) \subseteq \mathscr{P}(A \cup B)$
    \end{theorem}

    \begin{proof}
        $\mathscr{P}(A \cup B) = \{x\mid x \subseteq A \cup B\}$

        $\forall x \in \mathscr{P}(A) \cup  \mathscr{P}(B), x  \in \mathscr{P}(A) \vee x \in \mathscr{P}(B)$

        Then $x \subseteq A \vee x \subseteq B$

        Then $x \subseteq A \cup B$
        \begin{remark}
            $x \subseteq A \vee x \subseteq B \implies x \subseteq A \cup B$ is not a iff statement
        \end{remark}
    \end{proof}
        
    \begin{theorem}
        $\neg \exists A \forall x, \{x\} \in A $
    \end{theorem}

    \begin{proof}
        Assume A exists as set $A = \{\{x\} \mid x \}$
        
        By Union Axiom $\bigcup A$ is a set, $\bigcup A = \{y \mid (\exists b \in A)y\in b\}$

        Then $\bigcup A = \{x\mid x\}$

        Which is a proper class rather than set By Theorem 2A on page 22
    \end{proof}

    \begin{theorem}
        $A \in B \implies \mathscr{P}(A) \in \mathscr{P}(\mathscr{P}(\bigcup B))$
    \end{theorem}
        
    \begin{proof}
        $A \in B \implies A \subseteq \bigcup B$ By Theorem 5

        $\implies \mathscr{P}(A) \subseteq \mathscr{P}(\bigcup B)$ By Theorem 1

        Thus $\mathscr{P}(A) \in \mathscr{P}(\mathscr{P}(\bigcup B))$ By def of $\mathscr{P}(A)$
    \end{proof}

    \begin{remark}
        Boolean Algebra and Set Algebra

        \vspace{0.5em}
        \noindent
        \begin{tabularx}{\textwidth}{X | X}
            $\cup$& $\vee$ \\
            $A \cup B = B \cup A$ -commutative& $A \vee B = B \vee A$ -commutative\\
            $A \cup (B \cup C) = (A \cup B) \cup C$ -associative & $A \vee (B \vee C) = (A \vee B) \vee C$ -associative\\
            $A \cup \emptyset = A$ -identity & $A \vee 0 = A$ -identity\\
            ~ & ~ \\
            $\cap$& $\wedge$\\
            $A \cap B = B \cap A$ -commutative& $A \wedge B = B \wedge A$ -commutative\\
            $A \cap (B \cap C) = (A \cap B) \cap C$ -associative & $A \wedge (B \wedge C) = (A \wedge B) \wedge C$ -associative\\
            $A \cap U = A$ -identity & $A \wedge 1 = A$ -identity\\
            ~ & ~ \\
            $A \cup (B \cap C) = (A \cup B) \cap (A \cup C)$ & $A \vee (B \wedge C) = (A \vee B) \wedge (A \vee C)$ \\
            $\cup$ distributes over $\cap$ & $\vee$ distributes over $\wedge$ \\
            $A \cap (B \cup C) = (A \cap B) \cup (A \cap C)$ & $A \wedge (B \vee C) = (A \wedge B) \vee (A \wedge C)$ \\
            $\cap$ distributes over $\cup$ & $\wedge$ distributes over $\vee$ \\
            ~ & ~ \\
            $(A \cup B)^c = A^c \cap B^c$ & $\neg(A \vee B) = \neg A \wedge \neg B$ \\
            $(A \cap B)^c = A^c \cup B^c$ & $\neg(A \wedge B) = \neg A \vee \neg B$ \\
            ~ & ~ \\
            $A \cup A^c = U$ & $A \vee \neg A = 1$ \\
            $A \cap A^c = \emptyset$ & $A \wedge \neg A = 0$ \\
        \end{tabularx}
        \vspace{0.5em}
    \end{remark}

    \begin{remark}
        $A - B = A \cap B^c$
    \end{remark}

    \begin{theorem}
        $A = (A \cap B) \cup (A - B)$
    \end{theorem}

    \begin{proof}
        $(A \cap B) \cup (A - B) = (A \cap B) \cup (A \cap B^c)$

        $= A \cap (B \cup B^C)$

        $= A \cap U$

        $= A$
    \end{proof}

    \begin{remark}
        $A \times B = \{\{\{a\}, \{a ,b\}\} \mid a \in A \wedge b \in B\}$
    \end{remark}
    
    \begin{theorem}
        $A \times (B \cup C) = (A \times B) \cup (A \times C)$
    \end{theorem}

    \begin{proof}
        $A \times (B \cup C) = \{\{\{a\}, \{a ,b\}\} \mid a \in A \wedge b \in B \cup C\}$ By def

        $= \{\{\{a\}, \{a ,b\}\} \mid a \in A \wedge (b \in B \vee b \in C)\}$

        $= \{\{\{a\}, \{a ,b\}\} \mid (a \in A \wedge b \in B) \vee (a \in A \wedge b \in C)\}$

        $= \{\{\{a\}, \{a ,b\}\} \mid a \in A \wedge b \in B \}  \cup \{\{\{a\}, \{a ,b\}\} \mid a \in A \wedge b \in C\}$

        $= (A \times B) \cup (A \times C)$ By def
    \end{proof}

    \begin{axiom}
        Replacement Axiom
    \end{axiom}

    \begin{axiom}
        Infinity Axiom
    \end{axiom}
        
    \begin{axiom}
        Regularity Axiom
    \end{axiom}

    \begin{axiom}
        Choice Axiom
    \end{axiom}

    
    


\end{document}

